name: Create issue on commit
on:
- push
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: make test
      run: make test
